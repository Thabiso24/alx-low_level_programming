#include <stdio.h>
/**
 * main - Numbers of base
 * Return: 0 (Success)
 */

int main(void)
{
	char x;

	int q;

	x = 'a';
	q = 0;
	while
		(q < 10) {
			putchar(q);
			q++;
		}
	while
		(x <= 'f') {
			putchar(x);
			x++;
		}
	putchar('\n');
	return (0);
}
