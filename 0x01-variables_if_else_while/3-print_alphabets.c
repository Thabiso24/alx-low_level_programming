#include <stdio.h>

/**
 * main - Alphabetic lowercase to uppercase
 * Return: 0 (Success)
 */

int main(void)
{
	char b;

	char f;

	b = 'a';
	f = 'A';
	while
		(b <= 'z') {
			putchar(b);
			b++;
		}
	while
		(f <= 'Z') {
			putchar(f);
			f++;
		}
	putchar('\n');
	return (0);
{
