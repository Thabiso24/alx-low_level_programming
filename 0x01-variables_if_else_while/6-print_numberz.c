#include <stdio.h>
/**
 * main - Single digit numbers
 * Return: 0 (Success)
 */

int main(void)
{
	int f;

	f = 0;
	while
		(f < 10) {
			putchar(f + '0');
			f++;
		}
	putchar('\n');
	return (0);
}
